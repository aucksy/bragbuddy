# Built artifacts
*.apk
*.aab
*.ap_
*.dex
*.class

# Gradle / build
.gradle/
build/
local.properties
captures/

# Signing — never commit keys
keystore.properties
*.keystore
*.jks

# API keys / secrets — never commit (OpenRouter etc.)
secrets.properties
*.secrets

# IDE
.idea/
*.iml
.DS_Store

# Kotlin
.kotlin/

# Stray Windows 'Copy' duplicates
*- Copy.md

# Eval harness (Phase AI-0) — commit only the BASELINE reports; never the raw record
eval/report.md
eval/report.json
eval/golden/from-backup.pending.jsonl
bragbuddy-backup.json
*bragbuddy-backup*.json
